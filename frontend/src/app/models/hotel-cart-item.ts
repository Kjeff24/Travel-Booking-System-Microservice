export interface HotelCartItem{
    id: string,
    hotelName: string,
    location: string,
    roomType: string,
    price: number,
    categoryId: string,
    quantity: string,
    totalPrice: number
}