export interface FlightCartItem{
    id: string,
    departureCity: string,
    destinationCity: string,
    price: number,
    date: Date,
    categoryId: string,
    quantity: string,
    totalPrice: number
}