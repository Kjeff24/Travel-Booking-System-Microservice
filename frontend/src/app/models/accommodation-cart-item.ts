export interface AccommodationCartItem{
    id: string,
    location: string,
    type: string,
    capacity: string,
    price: number,
    categoryId: string,
    quantity: number,
    totalPrice: number
}