spring:
  application.name: category-service
  config:
    import: optional:configserver:${CONFIG_SERVER_URL}
  cloud.config.uri: ${CONFIG_SERVER_URL}
server:
  port: 0
#  address: ${CATEGORY_SERVICE_HOST_NAME}